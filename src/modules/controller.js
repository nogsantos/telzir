const PreDefined = require('./pre-defined');
const SpeakMore = require('./speakmore');
/**
 *
 */
class Controller {
    /**
     *
     * @param {*} origin
     * @param {*} destiny
     * @param {*} timer
     * @param {*} plan
     */
	constructor(origin, destiny, timer, plan) {

		// 	return new SpeakMore(origin, destiny);

		// return new PreDefined(origin, destiny);
	}
}

module.exports = Controller;
